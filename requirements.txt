# Runtime dependencies installed by setup.sh / setup.ps1 after PyTorch.
#
# Do not install this file in isolation for a fresh environment. The setup
# scripts intentionally install several ABI-sensitive packages first:
# - numpy==1.19.5
# - torch/torchvision/torchaudio CUDA 11.7 wheels
# - opencv-python-headless==4.5.5.64 with --no-deps
# - pytorch-lightning/torchmetrics/lightning utilities with --no-deps
#
# Keeping those outside this file prevents pip from resolving newer numpy,
# torch, OpenCV, or Lightning builds that are incompatible with this pinned
# inference stack.

# Core dependencies
# CRITICAL: numpy must stay <2.0 for scikit-learn 0.24.2 and this pinned image stack.
numpy==1.19.5
packaging
pyyaml
tqdm
easydict==1.9.0

# Scientific computing libraries
# scikit-image==0.18.3 is used because 0.17.2 lacks practical Python 3.9 wheels.
scikit-image==0.18.3
scikit-learn==0.24.2

# Machine learning frameworks
# NOTE: TensorFlow is intentionally NOT included here.
# Bokeh inference uses PyTorch; setup scripts install the exact CUDA 11.7 wheels.
kornia==0.5.0
timm==1.0.22

# Data processing
pandas<1.4
matplotlib<3.6
joblib

# Utilities
hydra-core==1.1.0
tabulate

# GUI / focus-stacking helpers
Pillow>=9.0
requests
beautifulsoup4
