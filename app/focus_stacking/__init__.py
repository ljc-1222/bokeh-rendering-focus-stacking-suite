"""Focus stacking (Laplacian pyramid fusion) module copied into `bokeh_rendering_and_focus_stacking_suite/`.

This package is a vendored copy of the implementation from
`Laplacian-Pyramid-Based-Focus-Stacking/core/` in this workspace, adapted so
that:
- input datasets live under `examples/focus/`
- caches / outputs live under `outputs/cache/focus/` and `outputs/focus/`
"""
